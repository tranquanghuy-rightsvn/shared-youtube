<%= link_to "<span class='fa fa-thumbs-down #{ video.dislike_voted_by?(current_user.id) if current_user }'></span>".html_safe, reaction_video_path(video, emotion_type: 1), method: :post, remote: true %>
